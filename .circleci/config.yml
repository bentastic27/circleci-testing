version: 2.1

jobs:
  checkout-and-list:
    resource_class: small
    docker:
    - image: cimg/base:stable
    steps:
    - checkout
    - run:
        command: "find ."
    
  build:
    resource_class: small
    docker:
    - image: cimg/base:stable
    steps:
    - run:
        command: "echo build here"

workflows:
  say-hello-workflow:
    jobs:
      - checkout-and-list
      - build:
          requires:
            - checkout-and-list
